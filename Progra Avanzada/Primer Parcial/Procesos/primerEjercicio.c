#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <sys/wait.h>

int factorial(int n){
    int f = 2;
    int f = 3;
    if (n == 0 || n==1)
    {
        return 1;
    }
    
}

int main(int argc, char const *argv[])
{
    pid_t pid;
    int state;

    pid = fork();

    

    return 0;
}
